@model IEnumerable<Penedating.Service.Model.PartnerQueryResult>
@{
    ViewBag.Title = "Status";
}
<div class="row">
    <div class="span12">
        <div class="hero-unit">
            <h1>Partner status</h1>
            <p>
                Here is a list of our partners and their current integration status
            </p>
        </div>
    </div>
</div>
<table class="table table-striped">
  <thead>
    <tr>
        <th>Partner url</th>
        <th>Status</th>
        <th>Error</th>
    </tr>
  </thead>
    <tbody>
        @foreach(var partner in Model)
        {
            var success = partner.Profiles.Any();
            <tr class="@(success ? "success" : "error")">
                <td>@partner.PartnerUri</td>
                <td>@(success ? "OK" : "Failed")</td>
                <td>@partner.PartnerError</td>
            </tr> 
        }
    </tbody>
</table>